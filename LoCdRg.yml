- name: Loops, conditions and Register
  hosts: all
  user: opc
  become: yes
  tasks:
    - name: Add several users
      yum:
        name: "{{ item }}"
        state: present
      loop:
       - telnet
       - httpd
      when: ansible_os_family == "CentOS"
